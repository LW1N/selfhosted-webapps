# Optional: only needed when pulling from a private container registry.
# See README for instructions on generating the dockerconfigjson value.
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull-secret
  namespace: demo
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: REPLACE_WITH_BASE64_DOCKER_CONFIG
