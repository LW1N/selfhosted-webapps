apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: demo
  labels:
    app: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
      annotations:
        kubectl.kubernetes.io/restartedAt: "2026-02-24T00:00:00Z"
    spec:
      # ── Uncomment if pulling from a private registry ──
      # imagePullSecrets:
      #   - name: ghcr-pull-secret
      containers:
        - name: web
          image: docker.io/lw1n/php-mysql-demo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: DB_HOST
              value: "mysql"
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_DATABASE
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PASSWORD
            - name: ADMIN_PASSWORD_HASH
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: ADMIN_PASSWORD_HASH
            - name: STANDARD_USERS_JSON
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: STANDARD_USERS_JSON
                  optional: true
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
