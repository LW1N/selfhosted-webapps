apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: php-mysql-demo
  namespace: flux-system
spec:
  image: docker.io/lw1n/php-mysql-demo
  interval: 5m
  # If Docker Hub rate-limits unauthenticated pulls, uncomment and
  # create the secret in flux-system namespace (see execution plan):
  # secretRef:
  #   name: dockerhub-credentials
