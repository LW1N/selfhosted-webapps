apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: php-mysql-demo
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: php-mysql-demo
  filterTags:
    pattern: '^main-(?P<ts>\d+)$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
